// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: pidgr/v1/render.proto

package pidgrv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/pidgr/pidgr-proto/gen/go/pidgr/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RenderServiceName is the fully-qualified name of the RenderService service.
	RenderServiceName = "pidgr.v1.RenderService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RenderServiceRenderBatchProcedure is the fully-qualified name of the RenderService's RenderBatch
	// RPC.
	RenderServiceRenderBatchProcedure = "/pidgr.v1.RenderService/RenderBatch"
)

// RenderServiceClient is a client for the pidgr.v1.RenderService service.
type RenderServiceClient interface {
	// Render a template for multiple users, streaming results as each completes.
	RenderBatch(context.Context, *connect.Request[v1.RenderBatchRequest]) (*connect.ServerStreamForClient[v1.RenderBatchResponse], error)
}

// NewRenderServiceClient constructs a client for the pidgr.v1.RenderService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRenderServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RenderServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	renderServiceMethods := v1.File_pidgr_v1_render_proto.Services().ByName("RenderService").Methods()
	return &renderServiceClient{
		renderBatch: connect.NewClient[v1.RenderBatchRequest, v1.RenderBatchResponse](
			httpClient,
			baseURL+RenderServiceRenderBatchProcedure,
			connect.WithSchema(renderServiceMethods.ByName("RenderBatch")),
			connect.WithClientOptions(opts...),
		),
	}
}

// renderServiceClient implements RenderServiceClient.
type renderServiceClient struct {
	renderBatch *connect.Client[v1.RenderBatchRequest, v1.RenderBatchResponse]
}

// RenderBatch calls pidgr.v1.RenderService.RenderBatch.
func (c *renderServiceClient) RenderBatch(ctx context.Context, req *connect.Request[v1.RenderBatchRequest]) (*connect.ServerStreamForClient[v1.RenderBatchResponse], error) {
	return c.renderBatch.CallServerStream(ctx, req)
}

// RenderServiceHandler is an implementation of the pidgr.v1.RenderService service.
type RenderServiceHandler interface {
	// Render a template for multiple users, streaming results as each completes.
	RenderBatch(context.Context, *connect.Request[v1.RenderBatchRequest], *connect.ServerStream[v1.RenderBatchResponse]) error
}

// NewRenderServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRenderServiceHandler(svc RenderServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	renderServiceMethods := v1.File_pidgr_v1_render_proto.Services().ByName("RenderService").Methods()
	renderServiceRenderBatchHandler := connect.NewServerStreamHandler(
		RenderServiceRenderBatchProcedure,
		svc.RenderBatch,
		connect.WithSchema(renderServiceMethods.ByName("RenderBatch")),
		connect.WithHandlerOptions(opts...),
	)
	return "/pidgr.v1.RenderService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RenderServiceRenderBatchProcedure:
			renderServiceRenderBatchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRenderServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRenderServiceHandler struct{}

func (UnimplementedRenderServiceHandler) RenderBatch(context.Context, *connect.Request[v1.RenderBatchRequest], *connect.ServerStream[v1.RenderBatchResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("pidgr.v1.RenderService.RenderBatch is not implemented"))
}
